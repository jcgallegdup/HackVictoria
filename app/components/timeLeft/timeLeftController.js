myApp.controller('timeLeftController', function($scope, $mdDialog, $http, $window, $q, $mdToast, $location) {
  var self = this,  j= 0, counter = 0;
  self.activated = true;
  self.determinateValue = 30; // take a value as input in final version
});